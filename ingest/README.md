# Ingestion

The first module will be the GB8 Folder Observer. It will create a JSON inventory, a readable summary, and warnings for uncertain classifications while treating source folders as read-only.
