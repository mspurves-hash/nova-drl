# v1.4.1 Changelog

- Added explicit `system_metadata` classification.
- Added parent-folder log inheritance regression coverage.
- Separated meaningful evidence counts from total assigned-file counts.
- Excluded `.picasa.ini`, `Thumbs.db`, `desktop.ini`, `.DS_Store`, and related metadata from extraction and repair knowledge.
- Added event-scoped and unit-scoped metadata counts.
- Added separate `system_metadata_bundle.json`.
- Updated the live GB8 pilot expectations from six unit-level items to zero meaningful unit-level items and two metadata files.
- Preserved `Unaccounted files: 0` as the primary safety requirement.
