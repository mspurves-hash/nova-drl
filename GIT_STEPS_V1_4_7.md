# Git steps — v1.4.7

1. Copy/extract the FLAT ZIP into the Windows Nova-DRL-Starter Git working directory.
2. Commit/push with GitHub Desktop.
3. On Ubuntu: `cd /opt/nova-drl && git pull`
4. Run tests, then `--status`, then `--plan-only` before the full enrichment.
