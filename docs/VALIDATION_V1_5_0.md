# Validation — v1.5.0

Validated synthetically for:

- full-folder manifest selection (not a 10% sample),
- odd/no-Traveler folder exception handling,
- `.picasaoriginals` exclusion,
- Roger `(2)` primary / `(1)` supporting behavior,
- strict RMA evidence grounding,
- strict Customer PO separation,
- `MSR 56889` => Mouser procurement reference, not manufacturer PN `56889`,
- unsupported `DGK52102` association rejected with literal evidence recovery of `55516`,
- procurement-only replacement rows filtered from parts knowledge.
